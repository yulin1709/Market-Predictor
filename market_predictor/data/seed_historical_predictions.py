"""
data/seed_historical_predictions.py — DEPRECATED. DO NOT RUN.

This script previously seeded the predictions table with synthetic data derived
from labelled_articles. It has been disabled because synthetic data produces
misleading accuracy and P&L metrics (circular logic: direction inferred from
the same price move used to judge correctness → artificial 100% accuracy).

The predictions table now contains ONLY real live predictions logged daily
by the pipeline. Historical performance metrics are based on actual signals
generated by the model on the day, with real entry/exit prices from yfinance.

If you need to populate historical predictions, run the pipeline daily and
let backfill_actuals.py fill in exit prices automatically.
"""

raise RuntimeError(
    "seed_historical_predictions.py is disabled. "
    "Use only real live predictions from the daily pipeline."
)
